age = input("How old are you? ")
height = input("How tall are you? ")
weight = input("How much do you weigh? ")

print(f"So, you're {age} old, {height} tall and {weight} heavy.")

#Study Drills

# 1. In terminal, run pydoc input or pyhton3.9 -m pydoc input and ready the documentation on on input

# 2. type q to quit if necessary

# 3. Look online to see what pydoc does
#similar to help documenation, explains a python function/syntax for you

# 4. Use pydoc to read

# - open : Opens a file to return a stream, file is either text or byte string giving the name (or file path if its not in the cwd)
# - file :
# - os : library used for getting directories to move, change, publish files
# - sys  
#     DESCRIPTION
    # This module provides access to some objects used or maintained by the
    # interpreter and to functions that interact strongly with the interpreter.

    # Dynamic objects:

    # argv -- command line arguments; argv[0] is the script pathname if known
    # path -- module search path; path[0] is the script directory, else ''
    # modules -- dictionary of loaded modules